<%= form_with(model: task, local: true) do |f| %>
  <%= render 'layouts/error_messages', model: f.object %>
  
  <%= f.label :status, "ステータス名"%>
  <%= f.text_field :status%>
  
  <%= f.label :content, "タスク名" %>
  <%= f.text_field :content %> 
  
  <%= f.submit "編集" %>
<% end %>